import 'package:firebase_core/firebase_core.dart';
//import firebase storage
import 'dart:io';

Future<String> saveProfilePic(File compressedImage) {
  //save compressed profile picture and return path to image.
}
